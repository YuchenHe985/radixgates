# RadixGates 本地配置模板
# 复制为 .env 并填写实际值：cp .env.example .env
#
# .env 已在 .gitignore 中，不会被提交到 git。

# ──────────────────────────────────────────
# SSH_TARGET
# ──────────────────────────────────────────
# demo 脚本（pd_demo.py / enterprise_qa_demo.py / streaming_demo.py）在
# 本地 Mac 上运行时，会通过 SSH 连到部署了 Docker 容器的 GPU 服务器，
# 把容器日志拉回本地 examples/logs/。
#
# 格式：user@host:port
#   user  — SSH 登录用户名（通常是 root）
#   host  — GPU 服务器公网 IP 或域名
#   port  — SSH 端口（默认 22，云服务器常用非标准端口）
#
# 示例：
#   SSH_TARGET=root@203.0.113.10:22
#   SSH_TARGET=root@203.0.113.10:22
#
SSH_TARGET=root@<server-ip>:<ssh-port>
